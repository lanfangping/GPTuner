The 'autovacuum_vacuum_threshold' knob specifies the minimum number of updated or deleted tuples (default: 50) required to trigger a VACUUM on a table, configurable in postgresql.conf or server command line, with per-table overrides via storage parameters.