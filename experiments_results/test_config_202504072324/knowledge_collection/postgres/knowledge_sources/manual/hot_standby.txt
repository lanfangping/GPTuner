The 'hot_standby' knob, when enabled (default: on), allows read-only queries during recovery in PostgreSQL, functioning only in archive recovery or standby mode and requiring a server restart to change.