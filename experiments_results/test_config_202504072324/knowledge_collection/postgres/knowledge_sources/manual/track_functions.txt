The 'track_functions' knob enables tracking of function call counts and time usage, with options 'pl' (procedural-language only), 'all' (includes SQL and C functions), or 'none' (default, disabled); superusers can modify this setting, but inlined SQL functions are never tracked.