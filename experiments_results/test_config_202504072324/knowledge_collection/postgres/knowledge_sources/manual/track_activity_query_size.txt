The knob 'track_activity_query_size' reserves **1024 bytes** (default) per active session to store the executing command's text for the `pg_stat_activity.query` field and can only be configured at server startup.