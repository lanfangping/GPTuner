The `hot_standby_feedback` parameter, when enabled (default: off), prevents query cancels on standbys by sending feedback to the primary at intervals no shorter than `wal_receiver_status_interval` (default: 10s), but may cause primary database bloat by delaying cleanup of dead rows.