The 'track_activities' knob, enabled by default, collects real-time command execution data (including start time) for each session, visible only to superusers, pg_read_all_stats roles, and session owners, posing no security risk.