The `log_min_duration_statement` parameter logs the duration of SQL statements that run for at least the specified time (e.g., 250ms), with 0 logging all statements, -1 disabling logging (default), and overriding `log_min_duration_sample` for queries exceeding the threshold.