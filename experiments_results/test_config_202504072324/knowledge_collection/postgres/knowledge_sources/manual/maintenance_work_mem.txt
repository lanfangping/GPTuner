The `maintenance_work_mem` knob specifies the maximum memory (default: **64MB**) for maintenance operations like VACUUM and CREATE INDEX, with a **1GB cap** for dead tuple collection, and should be set higher than `work_mem` but cautiously to avoid excessive allocation by autovacuum workers.