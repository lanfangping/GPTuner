Set the `bgwriter_lru_multiplier` to a value between 2.0 and 10.0, depending on the workload, where higher values (e.g., 8.0) are suitable for write-heavy workloads to reduce checkpoint pressure, and lower values (e.g., 2.0) are better for read-heavy workloads to conserve memory.