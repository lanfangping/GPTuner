To set the 'hot_standby' knob in PostgreSQL, you should configure it to `on` to enable hot standby, typically with an appropriate setting for `max_standby_streaming_delay` (e.g., 30 seconds) to balance read availability with replication performance.