To enable bitmap scans in PostgreSQL, set the `enable_bitmapscan` parameter to `on` by executing the SQL command `SET enable_bitmapscan TO 'on';` or by adding `enable_bitmapscan = on` in the `postgresql.conf` file, typically keeping it enabled unless benchmarks indicate otherwise, as it is already `on` by default.