To set the `log_lock_waits` parameter in PostgreSQL, configure it to **'on'** to enable logging of lock waits; you can also specify a threshold for logging by setting the `deadlock_timeout` parameter to values like **'1s'** or **'2s'** to log locks that exceed these durations.