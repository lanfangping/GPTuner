The 'enable_partition_pruning' knob, which defaults to "on," allows the query planner to eliminate irrelevant partitions from query plans and enables the executor to ignore these partitions during execution, optimizing performance on partitioned tables.