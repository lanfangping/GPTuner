The 'enable_partitionwise_join' knob controls whether the query planner uses partitionwise join for partitioned tables, which requires matching partition keys of the same data type; it is disabled by default due to potentially higher CPU and memory usage during planning.