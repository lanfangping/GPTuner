The `array_nulls` knob controls whether unquoted `NULL` is recognized as a null array element (default: on, allowing nulls), with PostgreSQL versions before 8.2 treating `NULL` as the string "NULL" instead, requiring this setting to be turned off for backward compatibility.