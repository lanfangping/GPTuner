The 'enable_partitionwise_join' knob toggles the query planner's ability to perform partitionwise joins between partitioned tables with matching partition keys of the same data type, but defaults to off due to potentially increased CPU and memory usage during planning.