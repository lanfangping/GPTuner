The `log_directory` parameter specifies the directory where log files are created when `logging_collector` is enabled, defaulting to the `log` subdirectory relative to the cluster data directory, and must be set in `postgresql.conf` or on the server command line.