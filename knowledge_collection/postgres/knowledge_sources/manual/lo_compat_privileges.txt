The `lo_compat_privileges` knob, when set to `on`, disables PostgreSQL 9.0's new large object privilege checks, reverting to pre-9.0 behavior where all users could read and write large objects, with the default being `off` and only adjustable by superusers.