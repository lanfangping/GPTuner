Set the `track_io_timing` knob in PostgreSQL to `on` (1) if you want to monitor I/O timing for queries, which can help identify performance bottlenecks, but be aware that enabling it incurs a small overhead (typically 1-5% depending on workload).