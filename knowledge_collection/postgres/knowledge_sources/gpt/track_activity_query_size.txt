Set the `track_activity_query_size` knob in PostgreSQL to a value that accommodates the longest query expected in your workload, typically between **1024 and 4096 bytes**, with **1024 bytes** being the default; increase it if queries exceed this size to avoid truncation in activity monitoring.