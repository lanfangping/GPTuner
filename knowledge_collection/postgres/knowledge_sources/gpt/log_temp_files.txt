Set the `log_temp_files` knob in PostgreSQL to a value (in kilobytes) that captures temporary file activity relevant to your workload, such as `1024` (1MB) to log files larger than 1MB, or `0` to log all temporary files, depending on your debugging or monitoring needs.