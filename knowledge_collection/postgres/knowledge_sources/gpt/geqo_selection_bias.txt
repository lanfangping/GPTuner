Set the `geqo_selection_bias` knob in PostgreSQL to a value between 1.5 and 2.0 (default is 2.0) to control the genetic algorithm's selection pressure, where lower values favor diversity and higher values favor the fittest individuals in query optimization.