Set the `log_truncate_on_rotation` knob in PostgreSQL to `on` (1) to overwrite existing log files when rotating, ensuring logs don't accumulate, or `off` (0) to append to existing logs, preserving historical data; choose based on storage capacity and retention needs.