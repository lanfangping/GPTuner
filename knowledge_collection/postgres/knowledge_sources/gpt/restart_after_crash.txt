Set the `restart_after_crash` knob in PostgreSQL to `on` (default) to automatically restart the server after a crash, or `off` if manual intervention is preferred, ensuring minimal downtime by allowing the server to recover within seconds to minutes depending on the crash context.