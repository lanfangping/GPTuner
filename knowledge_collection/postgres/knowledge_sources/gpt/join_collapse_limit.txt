Set the `join_collapse_limit` knob in PostgreSQL to a value between 8 and 20 (default is 8) to control the number of join combinations considered during query planning, increasing it for complex queries with many joins but keeping it lower to avoid excessive planning time.