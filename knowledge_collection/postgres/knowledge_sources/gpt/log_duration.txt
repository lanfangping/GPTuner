Set the `log_duration` knob in PostgreSQL to `on` (1) if you want to log the duration of every completed statement, or `off` (0) to disable it, with the default being `off`; this is useful for debugging slow queries but can generate significant log volume if enabled in high-traffic environments.