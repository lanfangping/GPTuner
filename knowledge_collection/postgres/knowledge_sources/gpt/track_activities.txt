Set the `track_activities` knob in PostgreSQL to `on` (default) to enable monitoring of current SQL commands for all sessions, or to `off` to disable it, with no specific numerical value required as it is a boolean parameter.