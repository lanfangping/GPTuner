Set the `enable_partitionwise_join` knob in PostgreSQL to 'on' if you have partitioned tables and expect the joined tables to benefit from partition-wise joins, especially in systems where you can allocate substantial resources (CPU, memory) to optimize query performance across large segmented datasets.