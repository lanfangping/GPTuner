Set the `enable_partitionwise_join` knob to `on` when the number of partitions is large (e.g., more than 100) and the query performance benefits from partition-wise joins, which can significantly reduce the amount of data processed.