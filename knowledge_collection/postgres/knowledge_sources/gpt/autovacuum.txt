For the 'autovacuum' knob in PostgreSQL, set the value based on the system's workload, typically starting with `autovacuum_vacuum_scale_factor = 0.2` and `autovacuum_vacuum_threshold = 50`, then fine-tune by adjusting these parameters according to the observed database activity and performance metrics.