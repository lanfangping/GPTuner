Set the `enable_sort` knob in PostgreSQL to `on` (default) to allow the query planner to use sorting operations, which is generally beneficial for performance, especially when the number of rows to sort exceeds 10,000.