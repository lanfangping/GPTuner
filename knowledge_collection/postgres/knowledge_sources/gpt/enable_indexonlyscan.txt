For the 'enable_indexonlyscan' knob in PostgreSQL, set the value to 'on' (true), as it effectively allows the system to perform index-only scans when possible, potentially improving query performance by minimizing disk I/O; this setting typically yields benefits unless specific workload characteristics suggest otherwise.