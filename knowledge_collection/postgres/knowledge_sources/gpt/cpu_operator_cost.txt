Set the `cpu_operator_cost` knob in PostgreSQL based on the relative cost of CPU operations compared to disk I/O, typically ranging from **0.0025 to 0.01**, with a default of **0.0025**, where lower values favor CPU-intensive queries and higher values favor disk I/O-heavy operations.