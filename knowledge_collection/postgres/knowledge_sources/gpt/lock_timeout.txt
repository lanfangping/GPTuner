Set the `lock_timeout` value in PostgreSQL to a specific number of milliseconds (e.g., 50ms, 100ms, or 500ms) to limit the duration a query waits for a lock, preventing long-running queries from causing contention and improving overall system performance.