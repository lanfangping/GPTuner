Set `autovacuum_work_mem` to a value between 64MB and 1GB, depending on available system memory and workload, with 64MB as a starting point for most systems, and increase it if vacuum processes are slow or if there are frequent autovacuum worker memory warnings.