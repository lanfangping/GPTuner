Set the `enable_partition_pruning` knob to `on` (default) to allow PostgreSQL to optimize queries by eliminating unnecessary partitions, typically improving performance for partitioned tables, especially when querying large datasets with well-defined partition boundaries.