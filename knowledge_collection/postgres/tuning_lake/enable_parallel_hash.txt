Suggestion: Enable the 'enable_parallel_hash' feature to allow the query planner to use parallel hash in hash-join plans. This setting is enabled by default but will only be effective if hash-join plans are also enabled.