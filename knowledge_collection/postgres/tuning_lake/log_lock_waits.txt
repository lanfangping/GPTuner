Suggestion:
Enable the 'log_lock_waits' knob to log messages when a session's wait for a lock exceeds the 'deadlock_timeout' period, which is typically 1 second by default. Be aware that only superusers have the authority to modify this setting, and it is disabled by default.