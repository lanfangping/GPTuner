The 'enable_partition_pruning' knob controls the query planner's ability to eliminate or ignore partitions in a partitioned table during query planning and execution, with the default setting being enabled (on), optimizing performance by reducing unnecessary partition scans.