Set the `maintenance_work_mem` knob in PostgreSQL to a value that is approximately 10% of the total system memory, typically between 64MB and 1GB, depending on the workload and available RAM, to optimize maintenance operations like `VACUUM`, `CREATE INDEX`, and `ALTER TABLE`.