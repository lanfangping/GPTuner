Set the `enable_seqscan` knob to `off` (0) to disable sequential scans and force the use of indexes when query performance is critical, but only after ensuring indexes are properly optimized and tested, as disabling it can lead to suboptimal plans for large tables or poorly indexed queries.