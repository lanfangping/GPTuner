To set the 'max_wal_size' in PostgreSQL, it is recommended to configure it 2 to 3 times larger than the 'checkpoint_segments' value, typically between 1GB and 16GB, based on your system's workload and the checkpoint completion times to ensure efficient disk I/O and prevent excessive checkpointing.