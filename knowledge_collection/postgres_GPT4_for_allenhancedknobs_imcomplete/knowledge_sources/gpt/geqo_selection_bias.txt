For the PostgreSQL knob 'geqo_selection_bias', set the value between 1.5 and 2.0, where the default is 2.0, to control the bias towards selecting the better plans during genetic query optimization; higher values favor better plans more strongly.