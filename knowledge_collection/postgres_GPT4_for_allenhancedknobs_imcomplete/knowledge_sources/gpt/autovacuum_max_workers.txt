For the 'autovacuum_max_workers' setting in PostgreSQL, configure the value based on the number of CPU cores available, typically aiming for a number between 3 and 6, but not exceeding the total number of cores to avoid overloading the system.