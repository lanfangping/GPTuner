For the 'track_counts' knob in PostgreSQL, typically set it to 'on' (default value) to enable effective statistics gathering for optimization; however, monitor system performance as it could slightly affect performance if database changes frequently.