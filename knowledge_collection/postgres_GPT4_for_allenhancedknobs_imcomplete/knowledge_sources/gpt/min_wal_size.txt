For the 'min_wal_size' knob in PostgreSQL, set the value starting at a minimum of 80MB, and adjust upwards depending on your workload's transaction rate and the amount of data that can accumulate between checkpoints, typically not exceeding 2GB for most common workloads.