To optimally set the 'cpu_operator_cost' knob in PostgreSQL, adjust the value based on empirical query performance tests, starting from the default of 0.0025 and tweaking it incrementally, observing the effects on query execution times, especially for CPU-intensive operations.