The `backslash_quote` knob controls whether a backslash-escaped quote (`\'`) is allowed in string literals, with options: `on` (always allow), `off` (always reject), and `safe_encoding` (default, allow only if the client encoding prevents ASCII `\` within multibyte characters, mitigating SQL-injection risks).