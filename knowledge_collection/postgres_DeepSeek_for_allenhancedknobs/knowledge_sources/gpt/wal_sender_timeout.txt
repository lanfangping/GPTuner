Set the `wal_sender_timeout` knob in PostgreSQL to a value (e.g., 60 seconds) that balances timely detection of WAL sender failures with avoiding unnecessary timeouts, considering network latency and replication workload.