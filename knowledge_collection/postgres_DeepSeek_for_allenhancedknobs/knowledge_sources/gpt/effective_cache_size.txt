Set `effective_cache_size` to approximately 70-80% of the total available RAM for the system, e.g., if the system has 16GB of RAM, set it to around 11-13GB (e.g., `11GB` or `13000MB`), as this parameter estimates the portion of memory available for disk caching.