Set the `wal_receiver_timeout` knob in PostgreSQL to a value (e.g., 60 seconds) that balances between detecting a standby server failure promptly and avoiding unnecessary timeouts due to temporary network delays or high load, typically ranging from 30 seconds to several minutes depending on your network reliability and workload.