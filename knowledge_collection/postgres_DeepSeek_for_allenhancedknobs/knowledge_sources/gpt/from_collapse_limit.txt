Set the `from_collapse_limit` knob in PostgreSQL to a value between 8 and 12 (default is 8) to control the planner's threshold for collapsing subqueries into joins, balancing query optimization and planning time.