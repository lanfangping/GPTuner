For the `track_functions` knob in PostgreSQL, set the value to `all` (or `1`) to track all functions, `pl` (or `2`) to track only procedural language functions, or `none` (or `0`) to disable function tracking, depending on whether you need detailed statistics for query optimization or debugging.