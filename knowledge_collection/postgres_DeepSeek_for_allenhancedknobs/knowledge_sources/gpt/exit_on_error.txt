Set the `exit_on_error` knob in PostgreSQL to `on` (1) to terminate the session immediately upon encountering any error, or `off` (0) to allow the session to continue processing despite errors, depending on whether strict error handling or fault tolerance is preferred.