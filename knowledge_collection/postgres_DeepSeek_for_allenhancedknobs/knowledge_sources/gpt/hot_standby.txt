Set the `hot_standby` knob in PostgreSQL to `on` (1) to enable read-only queries on standby servers, ensuring high availability and load balancing, typically in replication setups where standby servers are used for failover or read scaling.