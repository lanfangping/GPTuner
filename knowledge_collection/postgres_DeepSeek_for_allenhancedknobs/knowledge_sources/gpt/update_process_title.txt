Set the `update_process_title` knob in PostgreSQL to `on` (1) if you want the process title to reflect the current command being executed, or `off` (0) to disable this feature, with `on` being the default and recommended setting for better process visibility.