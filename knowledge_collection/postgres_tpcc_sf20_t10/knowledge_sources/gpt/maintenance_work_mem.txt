Set `maintenance_work_mem` to 25% of the total RAM for typical workloads, but no more than 2GB to avoid excessive memory usage during maintenance operations, especially on systems with less than 8GB of RAM.